function splitAndAdd(arr, n) {
    half = Math.ceil(arr.length/2)
    rside = arr.slice(0,half)
    lside = Math.ceil(arr.length/2)
    return ()=>
    {
       
    }
  }

console.log((splitAndAdd([1,2,3,4,5], 2)))